# 后端 API 地址
VITE_API_BASE_URL=http://localhost:8000

# WebSocket 地址
VITE_WS_BASE_URL=ws://localhost:8000/ws

# 是否使用 Mock 回放（true=本地 Mock；false=连接后端 WebSocket 联调）
# 联调时复制为 .env.local 并设 VITE_USE_MOCK=false
VITE_USE_MOCK=true
